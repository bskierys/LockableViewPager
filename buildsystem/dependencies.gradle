allprojects {
    repositories {
        jcenter()
    }
}

Properties properties = new Properties()
properties.load(project.rootProject.file('gradle.properties').newDataInputStream())

def versionMajor = properties.getProperty('versionMajor')
def versionMinor = properties.getProperty('versionMinor')
def versionPatch = properties.getProperty('versionPatch')
def versionBuild = properties.getProperty('versionBuild')

ext {
    libraryMinSdkVersion = 9
    libraryTargetSdkVersion = 24
    libraryCompileSdkVersion = 24
    libraryBuildToolsVersion = "24.0.0"
    libraryVersionCode = versionMajor.toInteger() * 10000000 + versionMinor.toInteger() * 100000 + versionPatch.toInteger() * 1000 + versionBuild.toInteger()
    libraryVersionName = "${versionMajor}.${versionMinor}.${versionPatch}-b${versionBuild}"

    //Libraries
    androidSupportVersion = '24.2.1'
    junitVersion = '4.12'
    butterKnifeVersion = '8.4.0'
    robolectricVersion = '3.0'

    mobileDependencies = [
            // Android
            appcompat           : "com.android.support:appcompat-v7:${androidSupportVersion}",
            support             : "com.android.support:support-v4:${androidSupportVersion}",

            // ButterKnife
            butterknife         : "com.jakewharton:butterknife:${butterKnifeVersion}",
            butterknife_compiler: "com.jakewharton:butterknife-compiler:${butterKnifeVersion}",

            // Unit test dependencies
            junit               : "junit:junit:${junitVersion}",
            robolectric         : "org.robolectric:robolectric:${robolectricVersion}",
    ]
}